package com.example.ar.calibration.model

/**
 * Detection/synthesis origin for an application-level wall model.
 */
enum class WallSource {
    PLANE_DETECTION,
    FLOOR_CORNER_EXTRUSION,
    HYBRID
}
